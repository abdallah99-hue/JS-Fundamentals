const args = process.argv.slice(2); if (!args[0] || isNaN(parseInt(args[0], 10))) { console.log("Missing size"); } else { const size = parseInt(args[0], 10); for (let i = 0; i < size; i++) { console.log("X".repeat(size)); } }
